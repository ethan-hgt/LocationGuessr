version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/backend/node_modules
    environment:
      - NODE_ENV=development
    env_file:
      - backend/.env